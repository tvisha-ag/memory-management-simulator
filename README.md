# memory-management-simulator
OS memory management simulator implementing First Fit, Best Fit, and Worst Fit algorithms using a doubly-linked list with block coalescing — empirically proving that Best Fit produces the highest external fragmentation (82.6%) under heavy mixed workloads, contradicting its theoretical advantage.
